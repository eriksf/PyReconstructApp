name: Docker Image CI

on:
  push:
    branches: [ "main", "autoseg-env" ]
    tags: [ "v*" ]
  pull_request:
    branches: [ "main" ]

jobs:

  build-pyreconstruct:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v2

      - name: Set up Docker Buildx
        id: buildx
        uses: docker/setup-buildx-action@v2

      - name: Cache Docker layers
        uses: actions/cache@v3
        with:
          path: /tmp/.buildx-cache-pyreconstruct
          key: ${{ runner.os }}-buildx-pyreconstruct-${{ github.sha }}
          restore-keys: ${{ runner.os }}-buildx-pyreconstruct-
          
      - name: Docker metadata
        id: meta
        uses: docker/metadata-action@v4
        with:
          images: eriksf/pyreconstruct
          flavor: latest=true
          tags: |
            type=ref, event=branch
            type=ref, event=pr
            type=semver, pattern={{version}}

      - name: Login to DockerHub
        if: github.ref_type == 'tag'
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push image
        uses: docker/build-push-action@v4
        with:
          context: .
          platforms: linux/amd64
          push: ${{ github.ref_type == 'tag' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=local,src=/tmp/.buildx-cache-pyreconstruct
          cache-to: type=local,dest=/tmp/.buildx-cache-pyreconstruct-new

      - name: Move cache
        run: |
          rm -rf /tmp/.buildx-cache-pyreconstruct
          mv /tmp/.buildx-cache-pyreconstruct-new /tmp/.buildx-cache-pyreconstruct
